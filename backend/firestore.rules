rules_version = '2';

// All access goes through the backend's Firebase Admin SDK, which bypasses
// these rules entirely. Deny all direct client access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
